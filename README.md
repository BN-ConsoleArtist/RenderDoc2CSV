RenderDoc Draw Call Parser: Extracts Opaque and Translucent draw calls from Event Browser text file exports and excludes fixed draw calls from other passes such as post processing. Sorts scene objects by occurrence to easily identify worst offenders and writes to CSV file. Useful to help formulate optimization strategies. The render passes are configured for Unreal Engine mobile / Vulkan.
